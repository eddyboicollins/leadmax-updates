const GEMINI_CONFIG = {
  url: 'https://gemini.google.com/_/BardChatUi/data/assistant.lamda.BardFrontendService/StreamGenerate?bl=boq_assistant-bard-web-server_20260324.01_p0&f.sid=8477920910441249606&hl=en-US&_reqid=2208378&rt=c',
  headers: {
    accept: '*/*',
    'content-type': 'application/x-www-form-urlencoded;charset=UTF-8',
    cookie: '',
    origin: 'https://gemini.google.com',
    referer: 'https://gemini.google.com/',
    'user-agent': 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/145.0.0.0 Safari/537.36',
    'x-same-domain': '1',
    'sec-ch-ua': '"Chromium";v="145", "Not:A-Brand";v="99"',
    'sec-ch-ua-arch': '"x86"',
    'sec-ch-ua-bitness': '"64"',
    'sec-ch-ua-form-factors': '"Desktop"',
    'sec-ch-ua-full-version': '"145.0.7632.6"',
    'sec-ch-ua-full-version-list': '"Chromium";v="145.0.7632.6", "Not:A-Brand";v="99.0.0.0"',
    'sec-ch-ua-mobile': '?0',
    'sec-ch-ua-model': '""',
    'sec-ch-ua-platform': '"Linux"',
    'sec-ch-ua-platform-version': '""',
    'sec-ch-ua-wow64': '?0',
    'x-goog-ext-525001261-jspb': '[1,null,null,null,"fbb127bbb056c959",null,null,0,[4],null,null,1]',
    'x-goog-ext-525005358-jspb': '["47868A8E-7A59-4A72-B36F-E02B72D61BD1",1]',
    'x-goog-ext-73010989-jspb': '[0]',
    'x-goog-ext-73010990-jspb': '[0]',
  },
  post_data:
    'f.req=%5Bnull%2C%22%5B%5B%5C%22hi%5C%22%2C0%2Cnull%2Cnull%2Cnull%2Cnull%2C0%5D%2C%5B%5C%22en-US%5C%22%5D%2C%5B%5C%22%5C%22%2C%5C%22%5C%22%2C%5C%22%5C%22%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2C%5C%22%5C%22%5D%2C%5C%22!7O-l74vNAAbUw4bn9MNC4AIwrNm6xRo7AEABEArZ1O65MUv54JeZDi1zIgLWFmtIy5dAggBXINKw7EBVxZByoQKNVriIQi5TKJwTiIXNefgp7zfT8kosw8LXAgAAAKZSAAAABGgBB34AQdcwutCRTsSuMsOFif2Qi2dVCM7vMhmUdbk76_5H15MlJ4MRsoYbe8Jrg1QCkVxVRjw_9Z7f4tpvBz1rvSz3yFe8mQNj-5FhONxW63R3xELN1WT6e_Id_JLRNuVd6W3L_-DTb6l2lMXe14fI0Tp6rKg-SAZ_jgyQLoK4wMQ1LNqP4xFRg7UqrxM7tb0im0VKj1n_VcY5ZmtfL6vB8vlYfmemsnTMzE_fFDciY19jnRHHzI-6R80qxJepPXIb9qxwkbQVPjynIVQe4e016n9KTvcQLbKMGFBztolgN0zVijzl1Eu6J7jSTbm0ASIO-TwdUojMMLUl63MtsA8ftS9ewU4foDIpS0cwQokd7WICWeLURpoaK0tvU0heEKrq3NexoMbrQv0i0YN4bQjS8ykyImQozQDTqAHlsNlmA6lz1i66IWQFzL58FN5Ct93Rk9QDzUtFubtrgGKCkOU9p5-2mWIOAp2demJakWXars86u69NSzq0_Yp44e9BY6MByi6kTMQaMmF3_znF-PlodHxXDDXohzwOILKJAeJyX1Fs91l4N60XXhDHMTg1Fe_0kGLZfZYN09flW3U8m56xcKneA6H4suo-ixzLDggBIbJ8ZAahqD9Rnk7OhH2-QBJPts9bIGDTzCFHXwxnkTHNx5b-rIweYpATymlLEZeH6VAx4j_a1cYjrtlb7NVV0X8_rM-AHRyqaWTswrJ_6Wt5t3UpGhxREmk1f65Oj-S9xWaH3mLiOwfaLnqP8bedTfbo2Wvso4p3DWXTRSHkasDZZpl_bFbp3iOGgoTERaQGMekksBIKFiP9mYQgEC-Q4-D87ZbFoV2YZ_COt9RB1Blsf6MqmA5rhTUFZ7RaE7P-SEm1ILKnfjz29VppjQYebl7Reja1_y-Xw5hxWb4Qw8aBD_2leNSv4NMSm1dc4YyzbiKWXv2JpRffUawwY75GNz5Z1So2chX9fPP-DbE48u2YAFjXuvBlB_UgLYq8u1Zx8oRoePoG_PIxjzHWewFAVvFxyqlIrDSVJOEKBftWI3-A0dOhTF-QzB2uUzFnNI7DrvtPpaKHzCTlqgxGkQNJ_K3iINo6ZVqVo_gpHKMvHdrm7HAlSRJxQQtaGGc4oSoEojFb7yzWTfSI7V1hy8oVo1Cf-nG5oRXXsBbSB9L1L4OoA2DagJDmEtUD1Xz6sKNQ2VYcwBngFOG_fiC7hMPY_-JtSqyPCarxQ7Ijl293x-vxfXKnjvSc0a-nsU14%5C%22%2C%5C%22c49eb2d009a01e6f06edf592bcca6377%5C%22%2Cnull%2C%5B0%5D%2C1%2Cnull%2Cnull%2C1%2C0%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2C%5B%5B0%5D%5D%2C0%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2C1%2Cnull%2Cnull%2C%5B4%5D%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2C%5B2%5D%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2C0%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2C%5C%2247868A8E-7A59-4A72-B36F-E02B72D61BD1%5C%22%2Cnull%2C%5B%5D%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2Cnull%2C1%5D%22%5D&',
};

export const GEMINI_MODELS = {
  'gemini-3.6-flash': 'Gemini 3.6 Flash (Recommended)',
  'gemini-3.5-flash-lite': 'Gemini 3.5 Flash Lite',
  'gemini-3.1-pro-preview': 'Gemini 3.1 Pro',
  'gemini-2.5-flash': 'Gemini 2.5 Flash',
} as const;

export type GeminiModel = keyof typeof GEMINI_MODELS;

export type GeminiSource = 'proxy' | 'api';

export const DEFAULT_GEMINI_MODEL: GeminiModel = 'gemini-3.6-flash';

/** Stale stored model ids (e.g. retired previews) fall back to the default. */
export function normalizeGeminiModel(v: unknown): GeminiModel {
  return typeof v === 'string' && v in GEMINI_MODELS ? (v as GeminiModel) : DEFAULT_GEMINI_MODEL;
}

function buildUrl(url: string): string {
  const reqid = Math.floor(100000 + Math.random() * 900000);
  if (url.includes('_reqid=')) {
    return url.replace(/_reqid=\d+/, `_reqid=${reqid}`);
  }
  const separator = url.includes('?') ? '&' : '?';
  return `${url}${separator}_reqid=${reqid}`;
}

function buildPayload(basePayload: string, fullPrompt: string): string {
  const trimmed = basePayload.endsWith('&') ? basePayload.slice(0, -1) : basePayload;
  const params = new URLSearchParams(trimmed);
  const fReqRaw = params.get('f.req');
  if (!fReqRaw) return basePayload;

  try {
    const outer = JSON.parse(fReqRaw);
    if (Array.isArray(outer) && typeof outer[1] === 'string') {
      const inner = JSON.parse(outer[1]);
      if (Array.isArray(inner) && Array.isArray(inner[0])) {
        inner[0][0] = fullPrompt;
        outer[1] = JSON.stringify(inner);
      }
    }
    const nextFReq = JSON.stringify(outer);
    params.set('f.req', nextFReq);
    return `${params.toString()}&`;
  } catch {
    return basePayload.replace(/"hi"/, `"${fullPrompt.replace(/\\/g, '\\\\').replace(/"/g, '\\"')}"`);
  }
}

function parseGeminiText(streamText: string): string {
  let bestText = '';
  const cleaned = streamText.replace(")]}'", '');
  const lines = cleaned.split(/\r?\n/);
  for (const rawLine of lines) {
    const line = rawLine.trim();
    if (!line || !line.includes('wrb.fr')) continue;
    let parsedLine: unknown;
    try {
      parsedLine = JSON.parse(line);
    } catch {
      continue;
    }
    const arr = Array.isArray(parsedLine) ? parsedLine : [];
    const entries = arr[0] === 'wrb.fr' ? [arr] : arr.filter((item): item is unknown[] => Array.isArray(item) && item[0] === 'wrb.fr');
    for (const entry of entries) {
      const payload = entry[2];
      if (typeof payload !== 'string') continue;
      let inner: unknown;
      try {
        inner = JSON.parse(payload);
      } catch {
        continue;
      }
      if (Array.isArray(inner) && Array.isArray(inner[4])) {
        for (const candidate of inner[4]) {
          if (Array.isArray(candidate) && Array.isArray(candidate[1])) {
            const text = (candidate[1] as unknown[])
              .filter((p): p is string => typeof p === 'string')
              .join('')
              .trim();
            if (text.length > bestText.length) bestText = text;
          }
        }
      }
    }
  }
  return bestText;
}

/** Per-attempt network timeout: a hung proxy stream must become an error, never a silent hang. */
const PROXY_TIMEOUT_MS = 100000;

/** Temporary AI behavior log (proxy). Append-only (multi-process safe), rotated, never throws. */
export function aiLog(line: string): void {
  try {
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const fs = require('fs') as typeof import('fs');
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const path = require('path') as typeof import('path');
    const file = path.join(process.cwd(), 'ai_logs.txt');
    try {
      const st = fs.statSync(file);
      if (st.size > 2 * 1024 * 1024) {
        // rotate: keep the last ~500KB (best effort; a concurrent writer may re-add)
        const fd = fs.openSync(file, 'r');
        const buf = Buffer.alloc(512 * 1024);
        fs.readSync(fd, buf, 0, buf.length, Math.max(0, st.size - buf.length));
        fs.closeSync(fd);
        let tail = buf.toString('utf8');
        const nl = tail.indexOf('\n');
        tail = (nl >= 0 ? tail.slice(nl + 1) : tail) + '\n--- rotated ---\n';
        fs.writeFileSync(file, tail);
      }
    } catch { /* file may not exist yet */ }
    fs.appendFileSync(file, `${new Date().toISOString()} ${line}\n`);
  } catch { /* logging must never break AI calls */ }
}

function timeoutError(err: unknown, label: string): Error {
  const e = err instanceof Error ? err : new Error(String(err));
  if (e.name === 'AbortError') {
    return new Error(`${label} timed out after ${Math.round(PROXY_TIMEOUT_MS / 1000)}s (no bytes settled)`);
  }
  return e;
}

export async function askGemini(
  prompt: string,
  _apiKey: string,
  _model: GeminiModel = DEFAULT_GEMINI_MODEL,
  source: GeminiSource = 'proxy',
): Promise<string> {
  if (source === 'api') {
    return officialAskGemini(prompt, _apiKey, _model);
  }
  const url = buildUrl(GEMINI_CONFIG.url);
  const payload = buildPayload(GEMINI_CONFIG.post_data, prompt);
  const started = Date.now();
  aiLog(`[proxy] attempt start promptChars=${prompt.length}`);

  let response: Response;
  try {
    response = await fetch(url, {
      method: 'POST',
      headers: GEMINI_CONFIG.headers,
      body: payload,
      signal: AbortSignal.timeout(PROXY_TIMEOUT_MS),
    });
  } catch (err) {
    const ms = Date.now() - started;
    aiLog(`[proxy] fetch FAILED after ${ms}ms: ${(err instanceof Error ? err.name + ': ' : '')}${err instanceof Error ? err.message : String(err)}`);
    throw timeoutError(err, 'Gemini proxy request');
  }

  if (!response.ok) {
    aiLog(`[proxy] HTTP ${response.status} after ${Date.now() - started}ms`);
    throw new Error(`Gemini proxy returned HTTP ${response.status}`);
  }

  let text: string;
  try {
    text = await response.text();
  } catch (err) {
    aiLog(`[proxy] body-read FAILED after ${Date.now() - started}ms: ${err instanceof Error ? err.message : String(err)}`);
    throw timeoutError(err, 'Gemini proxy response read');
  }
  aiLog(`[proxy] HTTP 200 stream done in ${Date.now() - started}ms rawChars=${text.length}`);
  const parsed = parseGeminiText(text);
  aiLog(`[proxy] parsed chars=${parsed.length}`);
  if (!parsed) throw new Error('Gemini returned an empty response.');

  return parsed;
}

/** Calls the official Gemini REST API (generativelanguage.googleapis.com) with the user's API key. */
export async function officialAskGemini(
  prompt: string,
  apiKey: string,
  model: GeminiModel = DEFAULT_GEMINI_MODEL,
): Promise<string> {
  if (!apiKey) {
    throw new Error('No Gemini API key configured. Add it in Settings or switch the AI source back to Proxy.');
  }
  const url = `https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent?key=${encodeURIComponent(apiKey)}`;
  const started = Date.now();
  let response: Response;
  try {
    response = await fetch(url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        contents: [{ role: 'user', parts: [{ text: prompt }] }],
        generationConfig: { temperature: 0.9, topP: 0.95 },
      }),
      signal: AbortSignal.timeout(PROXY_TIMEOUT_MS),
    });
  } catch (err) {
    aiLog(`[api:${model}] fetch FAILED after ${Date.now() - started}ms: ${err instanceof Error ? err.message : String(err)}`);
    throw timeoutError(err, 'Gemini API request');
  }

  if (!response.ok) {
    const errText = await response.text().catch(() => '');
    let message = `Gemini API returned HTTP ${response.status}`;
    try {
      const j = JSON.parse(errText);
      if (j?.error?.message) message = j.error.message;
    } catch { /* keep default */ }
    throw new Error(message);
  }

  const data = (await response.json()) as {
    candidates?: Array<{ content?: { parts?: Array<{ text?: string }> } }>;
  };
  const text = data?.candidates?.[0]?.content?.parts
    ?.filter(p => typeof p?.text === 'string')
    .map(p => p.text as string)
    .join('')
    .trim();
  if (!text) throw new Error('Gemini returned an empty response.');
  return text;
}

export async function retryAskGemini(
  prompt: string,
  apiKey: string,
  model: GeminiModel,
  maxRetries = 4,
  onRetry?: (attempt: number, maxRetries: number, error: string) => void,
  source: GeminiSource = 'proxy',
): Promise<string> {
  let lastError: Error | null = null;
  aiLog(`[retry:${source}:${model}] start maxRetries=${maxRetries} promptChars=${prompt.length}`);
  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      const out = await askGemini(prompt, apiKey, model, source);
      aiLog(`[retry:${source}:${model}] SUCCESS on attempt ${attempt + 1} outChars=${out.length}`);
      return out;
    } catch (err) {
      lastError = err instanceof Error ? err : new Error(String(err));
      const isLastAttempt = attempt >= maxRetries;
      const msg = lastError.message;
      onRetry?.(attempt + 1, maxRetries + 1, msg);
      console.error(`[Gemini] Attempt ${attempt + 1}/${maxRetries + 1} failed: ${msg}`);
      aiLog(`[retry:${source}:${model}] attempt ${attempt + 1} FAILED: ${msg}`);
      if (isLastAttempt) break;
      const delay = Math.min(1000 * Math.pow(2, attempt), 15000);
      console.error(`[Gemini] Retrying in ${delay}ms...`);
      await new Promise(r => setTimeout(r, delay));
    }
  }
  aiLog(`[retry:${source}:${model}] ALL ATTEMPTS FAILED: ${lastError?.message}`);
  throw lastError || new Error('Gemini request failed after retries');
}
